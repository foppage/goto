name: Build and Deploy

on:
  push:
    branches:
      - 'main'
  pull_request:
    branches:
      - 'main'

jobs:
  build-and-deploy:
    runs-on: docker

    steps:
      - name: Add SSH private key
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          mkdir ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ./private.key
          chmod 600 ./private.key

      - name: Run Docker Compose
        env:
          SERVER_IP: ${{ vars.SERVER_IP }}
          USER: ${{ vars.USER }}
        run: ssh -o "StrictHostKeyChecking no" -i ./private.key $USER@$SERVER_IP "cd ~/goto && git pull && docker compose up -d --build"
